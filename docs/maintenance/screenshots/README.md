# Portfolio UI Screenshots

这个目录保留个人站点的设计过程和 QA 证据，避免将生成截图散落在 GitHub Pages 根目录。

- `ui-audit/`：审计和头脑风暴截图。
- `ui-tests/`：页面和响应式测试截图。
- `ui-verification/`：功能或视觉修复的验证截图。
- `ui-iterations/`：未归入特定审计的界面迭代过程。

新的自动化运行产物仍应写入已忽略的 `output/`、`.playwright-*` 或 `test-results/`；只有需要长期保留的证据才迁入本目录。
